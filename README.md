# Sudoku

An OpenAI gym environment for sudoku.

# Install

`pip install gym`
`pip install gym-sudoku`

# Usage

```python
import gym
import gym_sudoku
env = gym.make('Sudoku-v0')
```

# Release

`tools/release.sh`
